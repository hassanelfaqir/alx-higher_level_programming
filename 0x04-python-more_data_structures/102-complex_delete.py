#!/usr/bin/python3
def complex_delete(a_dictionary, value):
    list_k = list(a_dictionary.keys())

    for value_diction in list_k:
        if val == a_dictionary.get(value_diction):
            del a_dictionary[value_diction]

    return (a_dictionary)
